defaults:
  - default_entity_client

cls: vivarium.environments.components.entities.particle_lenia.ParticleLeniaController

controller_kwargs:
  color:
    _all_values_: yellow

renderer_kwargs:
  shape: circle
  line_width: 0.1
