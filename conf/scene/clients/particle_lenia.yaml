defaults:
  - default_entity_client

interface_cls: vivarium.components.entities.particle_lenia.ParticleLeniaInterface

controller_kwargs:
  color:
    _all_values_: yellow

renderer_kwargs:
  shape: circle
  line_width: 0.1
