defaults:
  - default_entity_client

controller_cls: vivarium.environment.components.entities.braitenberg.BraitenbergController
interface_cls: vivarium.environment.components.entities.braitenberg.BraitenbergInterface

controller_kwargs:
  color:
    _all_values_: red
  visible_wheels:
    _all_values_: true
  visible_proxs:
    _all_values_: true
  shape:
    _all_values_: square
