defaults:
  - session_defaults
  - _self_

scene_name: session_2

environment:
  components:
    component_list:
      objects:
        n_exists: 1

subtype_labels:
  - agent
  - object

interface:
  notebook:
    path: notebooks/sessions/session_2.ipynb

